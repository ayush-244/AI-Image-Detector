[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
  "venv",
  ".venv",
  "node_modules",
  "dist",
  "data",
  "backend/uploads",
  "backend/outputs"
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
